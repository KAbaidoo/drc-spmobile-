CREATE TABLE healthAreas (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    health_zone_id INTEGER
);

getByHealthZoneId:
SELECT *
FROM healthAreas
WHERE health_zone_id = :health_zone_id;

getAllHealthAreas:
SELECT *
FROM healthAreas;

insertHealthArea:
INSERT
INTO healthAreas
VALUES(?,?,?);
