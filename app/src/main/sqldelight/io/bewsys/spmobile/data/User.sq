CREATE TABLE user (
    username TEXT NOT NULL PRIMARY KEY,
    meta_data TEXT,
    password TEXT
 );

getByUsername:
SELECT *
FROM user
WHERE username = :username;

getAllUsers:
SELECT *
FROM user;

insertUser:
INSERT OR REPLACE
INTO user
VALUES(?, ?, ?);

deleteByUsername:
DELETE FROM user
WHERE username = :username;