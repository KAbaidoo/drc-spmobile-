CREATE TABLE territory (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    province_id INTEGER
);

getByProvinceId:
SELECT *
FROM territory
WHERE province_id = :province_id;

getAllTerritoriess:
SELECT *
FROM territory;

insertTerritory:
INSERT
INTO territory
VALUES(?,?,?);

